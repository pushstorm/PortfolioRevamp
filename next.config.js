/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com'], // 👈 add your image host/domain here
    },
  };
  
  module.exports = nextConfig;
  